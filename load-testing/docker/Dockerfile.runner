FROM mcr.microsoft.com/playwright/dotnet:v1.49.0-noble

# Keep only Chromium → huge size saving
RUN rm -rf /ms-playwright/firefox* /ms-playwright/webkit*

WORKDIR /app
COPY publish/ ./

ENV DOTNET_RUNNING_IN_CONTAINER=true \
    PLAYWRIGHT_BROWSERS_PATH=/ms-playwright \
    HEADLESS=true

ENTRYPOINT ["dotnet", "PlaywrightLoadTestRunner.dll"]
