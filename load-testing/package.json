{
  "name": "load-testing",
  "version": "1.0.0",
  "description": "This project is a scalable load testing framework using .NET 8, Playwright, and Reqnroll (SpecFlow).",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "generate": "node dist/index.js --generate",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:report": "npx playwright show-report report/playwright-report",
    "test:docker": "docker compose -f docker/docker-compose.yml down -v && docker compose -f docker/docker-compose.yml up --build",
    "test:docker:scale": "docker compose -f docker/docker-compose.yml down -v && docker compose -f docker/docker-compose.yml up --build --scale test-runner=3",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.41.0",
    "commander": "^13.1.0",
    "csv-writer": "^1.6.0",
    "dotenv": "^16.3.1",
    "fs-extra": "^11.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.10.0"
  }
}